spring:
  profiles:
    active: local

---
spring:
  config:
    activate:
      on-profile: local
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none
    database-platform: org.hibernate.dialect.MySQLDialect
  datasource:
    url: jdbc:mysql://localhost:3306/song_game
    username: root
    password: test1234
    driver-class-name: com.mysql.cj.jdbc.Driver


#logging:
#  level:
#    org.hibernate.SQL: debug
#    org.hibernate.orm.jdbc.bind: trace
#    org.springframework.data.redis: DEBUG
#    org.redisson: DEBUG
#    io.netty: DEBUG

cors:
  allowed-origins: http://localhost:5173, http://songquiz.duckdns.org:5173

---
spring:
  config:
    activate:
      on-profile: test
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop

cors:
  allowed-origins: http://70.12.50.220:5173, http://songquiz.duckdns.org:5173
