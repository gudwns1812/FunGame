# Build & Output
build/
out/
bin/
.apt_generated/
.classpath
.factorypath
.project
.settings/

# Spring Boot Config (Sensitive Data)
# 프로필별 설정에는 DB 접속정보 등 비밀값이 들어가므로 계속 추적하지 않습니다.
# application.yml 은 비밀값 없는 공통 설정만 두고 버전 관리합니다.
application-*.yml
application-*.properties

# Testing
/src/test/resources/application-test.yml
