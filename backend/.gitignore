# Build & Output
build/
out/
bin/
.apt_generated/
.classpath
.factorypath
.project
.settings/

# Spring Boot 설정 파일은 버전 관리합니다.
# 비밀값은 파일에 직접 쓰지 말고 ${ENV_VAR} 자리표시자로 두세요.
# 실제 값은 서버의 .env 로 주입됩니다(루트 .gitignore 에서 제외 처리).

# Testing
/src/test/resources/application-test.yml
